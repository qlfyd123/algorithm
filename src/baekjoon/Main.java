package baekjoon;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {

    private static boolean smallWartering(int treeHeight) {
        if (treeHeight >= 1) {
            return true;
        }
        return false;
    }

    private static boolean bigWatering(int treeHeight) {
        if (treeHeight >= 2) {
            return true;
        }
        return false;
    }

    private static boolean waterTree(int[] treeTargetHeight) {
        int treeTargetHeightLength = treeTargetHeight.length;
        index
        while ()
    }


    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int treeNum = Integer.parseInt(br.readLine());

        int[] treeTargetHeight = new int[treeNum];


    }
}
